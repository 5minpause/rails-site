<div class="w-full">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
  <% end %>

  <div class="flow-root">
    <ul role="list" class="-mb-8">
      <%= render @articles, first_article: @articles.first %>
    </ul>
  </div>
</div>
